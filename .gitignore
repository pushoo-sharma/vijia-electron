node_modules
dist
dist-electron
out
*.log
.DS_Store
Thumbs.db

# Secrets / local env (Supabase keys, etc.)
.env
.env.local
.env.*.local

# Local browser bridge (session token — do not commit)
.vijia/
